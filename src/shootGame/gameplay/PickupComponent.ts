import { Component } from "../../entitySystem/Component"
import { PlayerController } from "../player/PlayerController"

export class PickupComponent extends Component {
    public pickup(player: PlayerController) {

    }
}